# Copie este arquivo para .env e preencha apenas no ambiente local/Render.
# Nunca envie o .env real para o repositorio.

# Backend
NODE_ENV=
PORT=
MONGO_URI=
MONGODB_DB=
FRONTEND_URL=

# Frontend publico gerado no build da Netlify
VITE_API_URL=
